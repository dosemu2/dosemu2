name: Manual Normal
on:
  workflow_dispatch:

jobs:
  manual_normal:
    name: Manual (Normal)
    uses: ./.github/workflows/ci-build.yml
    with:
      subtype: ''
      runtype: 'normal'
      FAILURE_SUFFIX: 'manual-normal'
