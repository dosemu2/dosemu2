name: Manual FULL
on:
  workflow_dispatch:

jobs:
  manual_full:
    name: Manual (FULL)
    uses: ./.github/workflows/test-suite-worker.yml
    with:
      bldtype: 'standard'
      runtype: 'full'
      os: 'ubuntu-24.04'
