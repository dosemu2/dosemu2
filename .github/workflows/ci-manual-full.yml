name: Manual FULL
on:
  workflow_dispatch:

jobs:
  manual_full:
    name: Manual (FULL)
    uses: ./.github/workflows/ci-build.yml
    with:
      subtype: ''
      runtype: 'full'
      FAILURE_SUFFIX: 'manual-full'
