name: Manual ASAN
on:
  workflow_dispatch:

jobs:
  manual_asan:
    name: Manual (ASAN)
    uses: ./.github/workflows/test-suite-worker.yml
    with:
      bldtype: 'asan'
      runtype: 'simple'
      os: 'ubuntu-24.04'
